import { MudaCorAdicionarDirective } from './muda-cor-adicionar.directive';

describe('MudaCorAdicionarDirective', () => {
  it('should create an instance', () => {
    const directive = new MudaCorAdicionarDirective();
    expect(directive).toBeTruthy();
  });
});
